int main()
{
    /* creation and assignment */
    pt x;
    printpt([1l, 2l]);
    x = [99l, 22l];
    printpt(x);

    /* ops: add, multiply, subtract */
    /* printpt([1l, 2l] + [1l, 2l]); */
    return 0;
}
